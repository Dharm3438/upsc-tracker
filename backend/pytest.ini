[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
# The Mongo client fixture is module scoped so the whole file shares one SRV
# lookup; its event loop has to live at least that long.
asyncio_default_fixture_loop_scope = module
